Place your game server package here.
Name the file: `game_server.zip`

The zip file should contain the game server executable at the root or properly structured so that when extracted to `spawner/game_server`, the configured `GAME_BINARY_PATH` is valid.
