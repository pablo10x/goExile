# ==========================================
# 🎮 Master Server Configuration
# ==========================================

# ------------------------------------------
# 🗄️ Database
# ------------------------------------------
# Path to the SQLite database file.
# Defaults to "database/registry.db" if not set.
DB_PATH=database/registry.db

# ------------------------------------------
# 🛡️ Authentication & Dashboard
# ------------------------------------------
# Administrator credentials for dashboard access.
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123

# Two-Factor Authentication (TOTP)
# Base32 encoded secret key.
# Use a tool like `gotp` or an online generator to create one.
# Example: JBSWY3DPEHPK3PXP
# If left empty, 2FA (Step 2) is disabled.
ADMIN_2FA_SECRET=

# Email Verification (Step 3)
# SMTP settings for sending verification codes.
# If SMTP_HOST is left empty, Email Verification (Step 3) is skipped.
#
# Option 1: Individual fields
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
SMTP_FROM=no-reply@example.com
#
# Option 2: Connection String (Overrides individual fields if set)
# Format: smtp://user:password@host:port
#
# Brevo Example (Note: @ in username must be encoded as %40):
# SMTP_URL=smtp://9dff9d001%40smtp-brevo.com:YOUR_API_KEY@smtp-relay.brevo.com:587
#
# SMTP_URL=smtp://apikey:your_key@smtp.sendgrid.net:587

# ------------------------------------------
# 🔑 Security
# ------------------------------------------
# Shared secret key for Spawner <-> Master communication.
# Must match the KEY configured in your spawners.
MASTER_API_KEY=change_me_to_a_secure_random_string

# Set to "true" to enforce strict security (HTTPS cookies, etc.)
PRODUCTION_MODE=false
